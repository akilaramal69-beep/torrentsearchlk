# --- Domain Configuration ---
# Your domain name (e.g., search.example.com) or :80 for IP access
DOMAIN_NAME=:80

# --- Frontend Configuration ---
# Internal URL for the frontend to reach Bitmagnet (docker-to-docker)
BITMAGNET_URL=http://bitmagnet:3333

# --- Bitmagnet Backend Configuration ---
# Secrets
POSTGRES_PASSWORD=postgrespassword
POSTGRES_USER=postgres
POSTGRES_DB=bitmagnet

# Public URL (if exposing Bitmagnet directly, optional)
# BITMAGNET_PUBLIC_URL=http://your-domain:3333

# Optional: TMDB API Key for enriching metadata
# TMDB_API_KEY=
